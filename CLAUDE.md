# Claude Instructions — Meal Finder

## Git Rules
- **Never** create new branches or worktrees
- **Never** run `git checkout -b`, `git worktree add`, or any branch-creation commands
- Always work directly on the **current branch** in the origin repository at `D:\Meal_finder`
